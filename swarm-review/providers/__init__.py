# Provider modules for direct API access
# Each module exposes: run(model, prompt, system=None, stream_callback=None)
# Returns: (full_text, model_id, usage_dict)
# usage_dict keys: input_tokens, output_tokens, thinking_tokens (optional)
