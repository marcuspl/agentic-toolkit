# <CAMPAIGN> — Tech-debt / deferred log

"Obvious but not-now" items spotted mid-campaign and consciously DEFERRED so the
loop stays focused. Separate from BOARD.md; revisit after the campaign lands.

Format: `- [ ] <area> — <what> — <why deferred> — (spotted <YYYY-MM-DD>, <id/commit>)`

## Open
- [ ] <area> — <what> — <why deferred> — (spotted <YYYY-MM-DD>, <ref>)

## Resolved
- [x] <area> — <what> — DONE <YYYY-MM-DD> (<commit/id>).
